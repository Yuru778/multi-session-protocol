## What changed, and why it was wrong before

<!-- One or two lines. If it fixes a rule that was wrong, say what that cost in practice. -->

## Did you run it?

<!-- Platform and shell, if this touches the script, the lock, or any platform claim.
     "Not run" is an acceptable answer here. An unmarked untested claim is not. -->

## Checklist

- [ ] Generic — no project names, personal paths, or real session names; `<me>` / `<peer>` / `<mailbox>`
- [ ] `README.md` and `README.zh-TW.md` both updated, if either was
- [ ] `sh -n scripts/watch-mailbox.sh` passes, if the script changed
